name: Build

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    name: Build OpenSnap
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build
        run: swift build
